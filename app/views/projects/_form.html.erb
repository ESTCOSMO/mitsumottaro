<%= simple_form_for @project do |f| %>
    <%= f.input :name %>
    <%= f.input :days_per_point %>
    <%= f.button :submit, class: %i(btn btn-primary) %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")), projects_path, :class => 'btn btn-default' %>
<% end %>
